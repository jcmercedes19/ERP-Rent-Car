rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Regla temporal para la prueba de Fase 1. Se endurecerá en la Fase 2 según SECURITY_MODEL.md
      allow read, write: if true;
    }
  }
}
